test:                              # 默认环境(RUN_ENV 未设时)
  base_url: "https://gorest.co.in/public/v2"
  token_env: "GOREST_TOKEN"        # 读哪个环境变量拿 token(与 prod 分离,凭据互不影响)

prod:
  base_url: "https://gorest.co.in/public/v2"
  token_env: "GOREST_PROD_TOKEN"   # 生产环境 token 变量名