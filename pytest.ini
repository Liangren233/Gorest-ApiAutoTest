[pytest]
# 自定义标记,可用 @pytest.mark.api 标注用例
markers = api
# 打印+详细+结果写 report/tmp+每次先清空旧结果
addopts = -s -v --alluredir=report/tmp --clean-alluredir
# 关闭 pytest 对非 ASCII test ID 的 \uXXXX 转义,让中文用例名正常显示
disable_test_id_escaping_and_forfeit_all_rights_to_community_support = true
