# API 自动化测试框架

基于 pytest + YAML 数据驱动接口自动化测试框架，支持多环境配置、跨用例变量关联、Allure 可视化报告。

## 技术栈

- pytest：测试引擎 + 参数化驱动
- PyYAML：用例数据管理（数据/代码分离）
- requests：HTTP 客户端
- python-dotenv：密钥管理（.env 不落 Git）
- Allure：可视化测试报告
- GoRest API：测试目标（Bearer Token 鉴权）

## 快速启动

1. 克隆项目
2. 安装依赖：pip install -r requirements.txt
3. 配置密钥：cp .env.example .env 然后填 GOREST_TOKEN
4. 运行测试：pytest
5. 看报告：allure serve report/tmp

## 目录结构

api-auto-test/
  .env.example
  .gitignore
  config/env.yaml
  core/client.py
  core/yaml_util.py
  core/assertor.py
  data/users.yaml
  tests/conftest.py
  tests/test_users.py
  pytest.ini
  requirements.txt
  README.md

## 核心设计

1. 数据驱动：用例写 YAML，pytest_generate_tests 参数化
2. 跨用例关联：session fixture 维护 vars_pool，${user_id} 引用
3. 多环境：env.yaml + --env=test/prod
4. 断言引擎：status + schema 两档
5. 密钥管理：python-dotenv 加载 .env，不进 Git
6. Allure：步骤分层 + 请求/响应附件

## 运行结果

tests/test_users.py::test_api[查询用户列表并提取首个ID] PASSED
tests/test_users.py::test_api[查询提取到的ID对应的用户] PASSED

## 扩展方向

- 失败重试 pytest-rerunfailures
- logging 日志模块
- Jenkins Pipeline + 企微通知
- posts / comments 模块